# Export the individual routers so they can be imported from src.app.router

# You can also create a combined router if needed
# combined_router = APIRouter()
# combined_router.include_router(auth_router, prefix="/auth", tags=["auth"])
# combined_router.include_router(basic_router, tags=["basic"])

